apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml

namespace: crs-do-prod

patchesStrategicMerge:
  - ingress-patch.yaml
  - configmap-patch.yaml
